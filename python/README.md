# Python package notes

Documentation has been consolidated into the root README.

- Full guide: [../README.md](../README.md)
- Bridge config sample: [examples/bridge-config.example.json](examples/bridge-config.example.json)

Quick local check:

```bash
uv run --project python agentctl --help
```
